import { render } from '@driponfleek/bankai-lib-react-unit-test-utils';
import MenuButtonSecondary from '../MenuButtonSecondary';

describe('<MenuButtonSecondary />', () => {
    it('should render without crashing', () => {
        render(<MenuButtonSecondary />);
    });

    // it('should ', () => {});
});
