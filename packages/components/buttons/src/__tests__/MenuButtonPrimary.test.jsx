import { render } from '@driponfleek/bankai-lib-react-unit-test-utils';
import MenuButtonPrimary from '../MenuButtonPrimary';

describe('<MenuButtonPrimary />', () => {
    it('should render without crashing', () => {
        render(<MenuButtonPrimary />);
    });

    // it('should ', () => {});
});
