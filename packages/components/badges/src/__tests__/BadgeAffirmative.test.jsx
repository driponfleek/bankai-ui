import { render } from '@driponfleek/bankai-lib-react-unit-test-utils';
import BadgeAffirmative from '../BadgeAffirmative';

describe('<BadgeAffirmative />', () => {
    it('should render without crashing', () => {
        render(<BadgeAffirmative />);
    });

    // it('should ', () => {});
});
