export const examplesCodeStr = `import { Avatar } from '@epr0t0type/bankai-ui-avatars';
import { BankaiUser } from '@epr0t0type/bankai-ui-icons';

const renderIcon = () => <BankaiUser />;

<Avatar imgURL={avatar} />
<Avatar text="DY" variant={VARIANTS.ROUNDED_SQUARE} />
<Avatar
    renderIcon={this.renderIcon}
    variant={VARIANTS.SQUARE}
/>`;

export const customComponentExampleCodeStr = `import { Button } from '@epr0t0type/bankai-ui-buttons';`;
