import { addons } from '@storybook/addons';
// import { themes } from '@storybook/theming';

addons.setConfig({
    name: 'Bankai UI',
    // panelPosition: 'right',
    showPanel: true,
    sidebar: {
        showRoots: true,
    },
    // theme: themes.light,
});
